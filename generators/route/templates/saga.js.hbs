/*
 *
 * {{properCase routeName}} Saga
 *
 * @author {{username}}
 * @date {{date}}
 */


import { all, put, call, takeLatest } from 'redux-saga/effects';
import * as actions from '@store/actions/{{camelCase routeName}}.js';
import * as api from '@api/{{camelCase routeName}}.js';


// @generator saga


// @generator saga:watch

export default function* rootSaga() {
	yield all([
		// @generator saga:fork
	]);
}
