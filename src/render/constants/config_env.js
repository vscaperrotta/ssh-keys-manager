const envSettings = window;

export const Config = {
  api_rest_be: envSettings.API_REST_BE,
	language_url: envSettings.LANGUAGE_URL,
	client_version: envSettings.CLIENT_VERSION,
	auth_service: envSettings.AUTH_SERVICE,
	token_service: envSettings.TOKEN_SERVICE,
  redirect_uri: envSettings.REDIRECT_URI,
};
